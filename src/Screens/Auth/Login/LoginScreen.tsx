import Login from '../../../Components/screen-components/Authentication/Login/Login';
const LoginScreen = ({navigation}: any) => {
  return <Login navigation={navigation} />;
};

export default LoginScreen;
